
export class Transform {
    constructor(x_pos=0, y_pos=0, width=0, height=0) {
        var x = x_pos;
        var y = y_pos;

        var w = width;
        var h = height;

        var rotation = angle;
    }
}